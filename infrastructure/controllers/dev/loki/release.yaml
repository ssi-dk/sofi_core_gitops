apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: loki
  namespace: sofi
spec:
  chart:
    spec:
      chart: loki
      version: "5.38.0" # pick a recent stable chart version
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system   # 👈 this is critical
  interval: 1h
  values:
    persistence:
      enabled: true
      size: 5Gi
